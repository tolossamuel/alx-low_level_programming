#include <stdio.h>

/**
 * main- print digits
 * the digits start from 0 up to 9
 * Return: zerro 0 (success)
 */
int main(void)
{
int i;

for (i = 0; i < 10; i++)
{
	printf("%d", i);
}
putchar('\n');
return (0);
}
