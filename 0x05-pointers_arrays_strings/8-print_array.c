#include "main.h"

/**
 * print_array - print elements of array
 * @a: array of element
 * @n: printed element length
*/
void print_array(int *a, int n)
{
	int i;

	for (i = 0; i < n; i++)
	{
		printf(*s[i]);
		if (i < (n - 1))
		{
			printf(", ");
		}
	}
	printf('\n');
}
